# Cineflow — инвентаризация функционала (чек-лист приёмки)

Составлено по коду `index.html` (15 850 строк, 1 260 022 байта) на коммите `6b9b528`.
**Сверено с кодом 2026-09-10.** Проверено 280 пунктов автоматическим поиском по маркерам —
все найдены. Ниже: поправки к первой редакции, найденные пропуски и подтверждённые дыры.

**Назначение:** после каждого этапа редизайна пройти по этому списку и подтвердить, что
каждый пункт на месте и достижим. Пункт, которого не стало, — брак, а не упрощение.
Действие может ПЕРЕЕХАТЬ, но остаётся достижимым за ≤2 нажатия и упоминается в отчёте
с новым адресом.

Обозначения: `[R]` — скрыто в режиме просмотра (`cf-edit-only`), `[!]` — есть подтверждение удаления.

---

## 0. Итог сверки

| Зона | Пунктов проверено | Найдено |
|---|---|---|
| Общая оболочка | 31 | 31 |
| Сцены | 64 | 64 |
| Галерея | 29 | 29 |
| Объекты | 24 | 24 |
| Доски | 78 | 78 |
| КПП | 12 | 12 |
| Экспликации | 25 | 25 |
| Модалки и компоненты | 17 | 17 |
| **Всего** | **280** | **280** |

Потерянного нет. Но первая редакция **не описывала содержимое четырёх больших модалок** —
там лежит около сорока действий, включая спасение рисунка и резервную копию. Они дописаны
в разделе 8. Без них чек-лист пропустил бы их пропажу.

### Поправки к номерам строк

| Пункт | Было в инвентаризации | На самом деле |
|---|---|---|
| Экран «КПП» | 14044 | **14057** |
| Таб-бар телефона | 14509 | **14510** |
| Шторка «Ещё» | 14548 | **14550** |
| Модалка «Агент КПП» | — | **15374** (`showAgentModal`) |

Остальные ссылки верны: Сцены 10571, Галерея 11631, Объекты 12019, Доски 12429,
Экспликации 14230, шапка 10455, Lightbox 2236, RefPicker 2687, SearchPalette 3029,
ConfirmModal 1621, DrawingModal 1763, LightSchemeEditor 2474, WelcomeModal 1653.

### Поправки по существу

- **Режимы доски зовутся `'draw'` и `'erase'`**, а не `'brush'`/`'eraser'`. Последние —
  это `tool` внутри DrawingModal (1830–1831), другой компонент. Кто будет искать кисть
  доски по слову `brush`, попадёт не туда.
- Подтверждение удаления гайда называется **«Удалить документ?»** (14248), а не «Удалить гайд».
- Схем света ДВЕ разных кнопки удаления с разными текстами: «Удалить схему?» (Экспликации,
  14258) и «Удалить схему света?» (вкладка на доске, 12542).
- Панель папок галереи — `setGalleryTagsOpen`; массовые действия — `bulkPutOnBoard`,
  `bulkDeleteRefs`, `bulkToScene`, `bulkToLocation`, `bulkAddTag`.
- Кнопки `−` / `+` / `✕` в панели техники и света сделаны одним `updateSceneNested`
  со сдвигом `-1` / `+1` / `-(текущее)`.

---

## 1. Общая оболочка

1. Шапка планшета/десктопа, 42 px (10455) — иконки 6 модулей + чат с бейджем непрочитанного, поиск «Поиск…» с `⌘K`, сегмент режимов сцены, точка синхронизации (клик = `kickSyncNow`), «···».
2. Меню «···» (10528): Проект · Доступ · Агент КПП · режимы сцены на узком экране · счётчик «N сцен · M кадров».
3. Шапка телефона (10398) — «‹ Сцены», компактные режимы, «···», имя модуля, счётчик, «Просмотр ?», точка связи, поиск.
4. Таб-бар телефона (14510): Сцены · Галерея · Объекты · Чат · Ещё. Повторный тап по активной «Сцены» возвращает к списку.
5. Шторка «Ещё» (14550): остальные модули, режимы экрана сцены, Проект / Доступ / Агент КПП.
6. Палитра поиска `⌘K` / `Ctrl+K` (в т. ч. русские «л/Л», 10228) — переход к сцене, кадру, документу.
7. Тост (10370). Полоса отката удаления «Вернуть», 15 с (10375), умеет уступать место тосту (`bottom: toast ? 76 : 24`).
8. Красная полоса сбоя облака (10348) + `[!]` «Сбой повторяется» → `repairCloudCache` (`allowRO: true` — единственное подтверждение, работающее в режиме просмотра). Аварийный экран ErrorBoundary (1518) с текстом ошибки, «Скопировать» и сбросом, сохраняющим комнату/имя/ключ.
9. Точка синхронизации: онлайн / офлайн («N правок ждут связи») / отправка.
10. Режим просмотра `READ_ONLY`: бейдж «Просмотр ?», модалка «Это режим просмотра», скрытие всего `cf-edit-only`; `?view=` списком страниц, `?board=` — одна доска.
11. Модалки: Проект, Доступ, Хранилище фото, Агент КПП, дубли при загрузке, результат OCR, «На какую доску». **Содержимое — раздел 8.**
12. Чат проекта: дровер справа на узком, колонка в потоке на широком.
13. Аварийные адреса: `?inkoff=1` — доска без рисунка (4689); `?fps=1` и галочка «Показывать плавность» — счётчик плавности.

---

## 2. Экран «Сцены» (notion, 10571–11629)

**Режимы экрана** (484): Творческий · Скаутинг · Съёмка · Презентация — переключаются из шапки, из «···», из шторки «Ещё»; помнятся в `cf_scene_mode`.

### Действия
1. Свернуть/развернуть список сцен.
2. Выбрать сцену кликом по строке.
3. `[R]` «+ сцена».
4. Правка номера сцены, `maxLength 12` (`updateSceneNumber` перенумеровывает кадры и приставку в названии).
5. Правка даты смены.
6. Меню «···» сцены: `[R]` «Собрать на доске» · «Экспорт сцены в PDF» · `[R][!]` «Удалить сцену».
7. Правка названия сцены (`updateSceneTitle` — забирает номер из названия).
8. Правка текста сцены (счётчик слов).
9. «развернуть все ⤢» / «свернуть ⤡» референсов; Esc сворачивает.
10. Клик по референсу → лайтбокс со скоупом «Сцена #N».
11. `[R]` «✕» на референсе — отвязать от сцены (остаётся в галерее).
12. `[R]` Плитка «+» загрузки картинок (PhotoSourcePicker: камера / плёнка / файлы).
13. `[R]` «из галереи» → RefPicker «Привязать к сцене».
14. «развернуть все ⤢» раскадровки.
15. Переключатель «кадры» / «текст».
16. `[R]` «+ кадр».
17. `[R]` «файл» на пустом кадре.
18. `[R]` «скетч» на пустом кадре → DrawingModal.
19. `[R][!]` «✕» на кадре.
20. Правка номера кадра, оптики, описания.
21. Табличный вид раскадровки: колонки Кадр / Оптика / Описание, «+ кадр текстом».
22. «Открыть» у документа сцены → экран «Экспликации».
23. `[R]` «+ заметка», правка текста, `[R]` «✕» отвязать, смена цвета (6 цветов).
24. Чипы связей: «Объект · …» → Объекты; «Доска сцены · N» → выбор доски; схема света → Экспликации.
25. Режим «Скаутинг»: фото точки → лайтбокс, `[R]` «+ фото с точки», заметки с выезда, живой компас «Солнце сейчас».
26. Режим «Съёмка»: кнопка «✓» 44×44 на кадре (снято / снять отметку), счётчик «снято X из Y», «окно света до HH:MM».
27. Режим «Презентация»: только просмотр, панели скрыты.
28. Правая панель, 4 вкладки: Экспликация и замысел · Камера и техника · Свет · Объект и солнце. Повторный тап сворачивает; «✕ Свернуть панель»; Esc когда панель поверх.
29. Правка экспликации.
30. «Расширить с ИИ» (перезаписывает поле).
31. Панель техники/света: правка названия позиции, «−», «+», `[R]` «✕» убрать из сцены, поле «Новая позиция…» + Enter, «+ техника»/«+ прибор», «весь каталог · N ⤢», «+» из каталога, `[R][!]` «✕» удалить из каталога.
32. Выбор схемы света + «Открыть».
33. Телефон, список: пилюли «Смена N / Все / Не готовы / НАТ / ИНТ», секции по объектам, «+ сцена».
34. Телефон, карточка: лента референсов до краёв, «развернуть →» текста, лента раскадровки, 4 плитки (Техника / Свет / Объект / Экспликация) → bottom sheet, «Собрать сцену на доске».

### Состояния
«Сцен пока нет» · «Сцена не выбрана» · «По этому фильтру сцен нет» · нет кадров в режиме «Съёмка» · нет заметок · нет техники · сцена без объекта · нет координат · нет даты смены · полярный день/ночь · «номер занят» · кадр без картинки · загрузка ИИ («Думаю…») · READ_ONLY · офлайн · синхронизация.

---

## 3. Экран «Галерея» (gallery, 11631–12017)

### Действия
1. Свернуть/развернуть колонку «Папки · теги» (`setGalleryTagsOpen`).
2. `[R]` «+» новая папка (prompt «Название папки»).
3. Выбор папки: «Все» / папка / «Без папки».
4. Поиск по тегу с datalist-подсказками; «Сбросить».
5. Клик по тегу в списке «Все теги» — переключение фильтра.
6. `[R]` «Загрузить референсы» (PhotoSourcePicker).
7. `[R]` «Вставить» из буфера (две кнопки: в колонке и чипом).
8. «Выделить все» / «Снять выделение».
9. `[R]` «+ Загрузить» на телефоне.
10. Ленты чипов папок и тегов на телефоне, чип «+ папка».
11. Клик по карточке: открыть лайтбокс либо переключить выделение (если что-то выделено).
12. `[R]` Кнопка-галочка выделения на карточке.
13. `[R][!]` Корзина на карточке.
14. `[R]` Иконка сцены — привязать/снять с активной сцены.
15. `[R]` Иконка доски — положить/убрать с текущей доски.
16. `[R]` Правка подписи (placeholder «Название референса...»).
17. `[R]` Селект папки, включая «+ новая папка…».
18. `[R]` Добавление тега (TagInput) и «✕» на чипе тега.
19. `[R]` Панель массовых действий: «на сцену #N →» (`bulkToScene`), «на доску» (`bulkPutOnBoard`), «в объект» (`bulkToLocation`), общий тег + «+ тег» (`bulkAddTag`), «в папку…», `[!]` корзина (`bulkDeleteRefs`), «✕» снять выделение.
20. Drag & drop файлов и ссылок на лист (ложится в открытую папку).
21. Ctrl+V вставка картинки.

### Состояния
Пустая папка · «Тегов пока нет» · выделение (акцентная рамка) · загрузка (тосты «Оптимизация N изображений...», «Читаю PDF...», «Панорама N из M...») · дубли (модалка «Такие картинки уже есть») · READ_ONLY.
**Дыра:** при нуле референсов пустого состояния нет — видно только «Все референсы · 0 из 0».

---

## 4. Экран «Объекты» (locations, 12019–12427)

### Действия
1. Свернуть/развернуть список объектов.
2. Выбор объекта.
3. `[R]` «+ объект».
4. «Отчёт по объектам (PDF)».
5. Меню «···»: «Экспорт объекта в PDF» · `[R][!]` «Удалить объект «…»?» (9006).
6. `[R]` Правка названия (папка фотографий переезжает следом).
7. «развернуть все ⤢» / «свернуть ⤡» фотографий.
8. Клик по фото → лайтбокс со скоупом «Объект · …».
9. `[R]` «✕» на фото — отвязать (остаётся в галерее).
10. `[R]` Правка подписи фото.
11. `[R]` «+ фото со скаутинга», `[R]` «вставить».
12. Карточка «Солнце»: компас, дуга, золотой час — только чтение.
13. «открыть карту →» (OpenStreetMap).
14. `[R]` Правка адреса, координат, питания, комментария.
15. `[R]` «по адресу» — геокодинг Nominatim («Ищу…»).
16. Селект схемы света + «Открыть».
17. `[R]` «+ привязать сцену» / «скрыть список ⤡», чипы сцен.
18. Клик по карточке сцены → экран «Сцены».
19. `[R]` «+ заметка», правка, `[R][!]` «✕», палитра цвета.
20. Drag & drop на лист объекта; Ctrl+V.

### Состояния
«Объектов пока нет» + пояснение · нет фотографий · нет координат · ошибка геокодинга · полярный день/ночь · «Сцен в проекте ещё нет» · «без кадров» · READ_ONLY.

---

## 5. Экран «Доски» (whiteboard, 12429–14055)

### Действия
1. Клик по вкладке доски; двойной клик — переименовать (12438).
2. `[R][!]` «✕» удалить доску (референсы/кадры/документы снимаются, а не удаляются) — только если досок больше одной.
3. `[R]` «+» новая доска.
4. Вкладки схем света; двойной клик — переименовать (12524); `[R][!]` «✕» «Удалить схему света?»; `[R]` «+ схема».
5. `[R]` «Положить на доску» → панель пикера (Референсы / Документы / Раскадровка / Сцены, «Выбрать из галереи…»).
6. `[R]` Стикер (12583). `[R]` Новый референс (12585).
7. Экспорт PNG. Экспорт SVG.
8. `[R][!]` «Очистить рисунок на доске?» (12598).
9. Панель инструментов (`.cf-tools`, 44 px, 13495): Выбор `select` · `[R]` Кисть **`draw`** · `[R]` Фигуры `shape` · `[R]` Ластик **`erase`** · `[R]` OCR `ocr` · `[R]` Связь `link` · Панорамирование `pan` · переключатель «палец/стилус» · `[R]` Отменить/Вернуть · `[R]` Копировать/Вставить.
10. Панель свойств фигуры: вид фигуры (6), «Выбрано: N», «Удалить», цвет линии (6 + свой), заливка, кегль, выравнивание, толщина, штрих, начертание, скругление, маршрут, наконечники, прозрачность, «Сгруппировать»/«Разбить», порядок «Вперёд»/«Назад», «за карточками», свернуть панель.
11. Панель ластика (размер). Панель кисти (Перо/Мягкая/Маркер, `BRUSH_SIZES`, цвет).
12. Мини-карта: клик/ведение, свернуть/развернуть.
13. Масштаб «−» / «%» / «+» (0.3–2.0).
14. Шаблоны пустой доски: «Схема света» · «Мудборд» · «Раскадровка · сц N» + чипы пропорции кадра.
15. Карточка сцены: «✕» убрать с доски, правка названия и экспликации, кружок связи.
16. Карточка референса: `[!]` корзина (из проекта), «✕» убрать с доски, загрузка в пустую, подпись у выделенной, кружок связи, resize (двойной клик — сброс ширины).
17. Карточка кадра: «✕», рамка в пропорции проекта, загрузка «кадр или скетч», правка описания, кружок связи.
18. Карточка документа: правка заголовка и текста, «⤢» открыть в Экспликациях, «✕», бейдж сцены (клик — отвязать), resize.
19. Карточка заметки: `[!]` «✕», правка текста, кружок связи, бейдж сцены, resize, палитра цветов.
20. Жесты: рамка выделения, двойной клик по пустому — текстовый блок, двойной по фигуре — подпись, перетаскивание, Shift+клик, Ctrl — отключить притяжку, кружок связи → перетаскивание, режим «Связь» в два тапа, щипок, колесо, Ctrl+колесо, drag&drop в точку броска, обратный конец стилуса = ластик, OCR-рамка.
21. Клавиши (6654–6670): Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y, Ctrl+] / Ctrl+[, Ctrl+G / Ctrl+Shift+G, Ctrl+C / Ctrl+V, Delete/Backspace, Escape.

### Состояния
Пустая доска (три шаблона) · пустые разделы пикера · `exportBusy` · OCR (busy, «В рамке нет рукописи», модалка «Рукопись прочитана») · READ_ONLY (остаются выбор, панорама, масштаб, мини-карта, экспорт) · область-ссылка `CF_BOARD_SCOPE` · выделение · множественный выбор.

---

## 6. Экран «КПП» (table, 14057–14228)

### Действия
1. «По сменам» / «Все сцены» (`tableSubView`, помнится в `cf_kpp_view`).
2. «Текст сцены из сценария» / «Скрыть текст сцены» — на карточку сцены (`openScript`).
3. Правка номера сцены прямо в строке смены.
4. Правка даты смены прямо в строке (переносит сцену в другой день).

### Состояния
«Сцен пока нет» · «СЦЕНЫ ВНЕ СМЕН» · «Дата не назначена» · «Описание отсутствует» · «Без названия» · дубликат номера (янтарный) · нет техники/света на смене · READ_ONLY (экран полностью читаем — основной сценарий по ссылке).

### Показывает
«СЦЕНА #N | Название», дата, экспликация, текст сценария, «Раскадровка / План смены», «СМЕНА: дата», «N сцен», «Итого техника:», «Итого свет:», «камера: », «свет: ».

---

## 7. Экран «Экспликации» (docs, 14230–14444)

### Действия
1. Выбор гайда.
2. `[R]` «Положить на текущую доску» / «Убрать с доски».
3. `[R][!]` Удалить гайд — подтверждение называется **«Удалить документ?»**; кнопка есть только если гайдов больше одного.
4. `[R]` «+ Создать гайд».
5. Выбор схемы света. `[R][!]` «Удалить схему?». `[R]` «+ Новая схема».
6. `[R]` Правка заголовка и содержания гайда.
7. «развернуть ⤢» / «свернуть ⤡» длинного текста (`docOpen`).
8. `[R]` «+ Добавить участника»; правка ФИО / роли / телефона; `[R]` удалить участника (**без подтверждения**, 14334).
9. `[R]` Правка названия схемы; `[R]` «Открыть графический редактор» → LightSchemeEditor.
10. Панель «Печать и экспорт» (сворачиваемая): «Весь план» · «Гайд: …» · «Схема света: …» · «Список техники».
11. `[R]` «Загрузить КПП или доки»; «Скачать» файла; `[R]` отправить в облако (`sendFileToCloud`); `[R][!]` удалить файл.

### Состояния
Гайд и схема взаимоисключающи · «Гайд пока пустой» · короткий текст (кнопка разворота не показывается) · единственный гайд (удаление скрыто) · схема без превью · «Схема не привязана ни к одной сцене» · панель печати свёрнута по умолчанию · ошибки загрузки файла (5 разных тостов) · READ_ONLY.

---

## 8. Модалки — содержимое (ДОПИСАНО при сверке)

Первая редакция перечисляла модалки одной строкой. Внутри них около сорока действий,
и без списка чек-лист не заметил бы их пропажи.

### 8.1 «Управление проектом» (15162–15373)
1. Ключ Gemini API: поле, показать/скрыть, ссылка «Получить бесплатно».
2. Выключатель «Ключи общие для комнаты» (`cf_share_keys`).
3. «Где хранить фотографии» → открывает «Хранилище фото».
4. Галочка «Показывать плавность» (`cf_fps`).
5. **«Пропал рисунок доски?»** — `findLostInk`: поиск копий на устройстве и в облаке (включая части `~2`…`~40`), список находок с числом штрихов и **весом**, пометка тяжёлых.
6. **«Скачать файлом»** каждой находки (`downloadRescued`) — главный путь спасения.
7. **«Положить на доску»** (`putRescued`) — закрывает окно, переключает на «Доски», наводит вид.
8. `pushInkToCloud` — отправить рисунок в облако.
9. **«Снять рисунок с этой доски»** (`clearBoardInk`) + подсказка про `?inkoff=1`.
10. **Резервная копия: скачать `CineFlow_Backup_ГГГГ-ММ-ДД.json`** (15235–15248) — сцены, раскадровка, референсы, заметки, доски, документы, группа, объекты, каталоги, схемы, файлы, растр досок (`canvases`), штрихи (`inks`), фигуры (`shapes`).
11. **«📂 Импортировать из JSON»** (15324) — он же принимает файл спасённого рисунка.
12. `[!]` «Сбросить всё (Новый проект)» → `resetProject`; два подтверждения: «Начать новый проект?» и «Начать новый проект в новой комнате?».

### 8.2 «Совместный доступ» (14945–15161)
1. Код комнаты + «Войти» (переезд в другую комнату, перезагрузка).
2. `inviteLink` «Позвать в этот проект» (с `?room=`) + «Скопировать».
3. `plainLink` (БЕЗ кода комнаты) + «Скопировать» — отдельной кнопкой намеренно.
4. Ссылка-просмотр на каждую страницу (`?view=`) + «Скопировать ссылку».
5. «Показать ОДНУ доску» — `?view=board&board={id}`.
6. **Файл-снимок** `exportSnapshot(scope, boardId)` — по областям (весь проект / галерея / одна доска).

### 8.3 «Где хранить фотографии» (14690–14912)
1. Dropbox: `connectDropbox` (OAuth PKCE), «Скопировать адрес возврата» (`copyRedirect`), «Как получить ClientID», список нужных разрешений.
2. Яндекс.Диск: `connectYandex`.
3. Nextcloud: адрес, логин, пароль приложения; предупреждение про CORS.
4. **«Проверить связь»** (`cloudCheck`) — приняли ли ключ.
5. **«Проверить загрузку»** (`runCloudTest`) — весь путь фотографии по шагам, включая публичную ссылку.
6. «Отключить» (`disconnectCloud`).
7. **«Уже загруженные фотографии» → перенести** (`migrateToCloud`) + «Остановить».
8. «Что ответило облако» — текст последней ошибки (`cloudError`).

### 8.4 «Агент импорта: КПП и сценарий» (15374–15762)
1. Загрузка КПП из Excel (`loadXLSX`, xlsx по требованию).
2. Загрузка сценария `.txt` / `.fountain` / `.fdx`; `[!]` «Загрузить текст сценария?».
3. Предпросмотр найденных сцен + «Импортировать сцены».
4. Разговор с ИИ: поле ввода, отправка, «агент думает…».
5. Предпросмотр правок «Агент предлагает изменить:» + «Применить» / «Не надо».
6. «Очистить» разговор.

### 8.5 Прочие модалки
- «Рукопись прочитана» (14614) — правка текста, новый гайд на доску, дописать в существующий.
- «Такие картинки уже есть» (14651) — дубли при загрузке.
- «На какую доску» (15129) — выбор доски, «+ Новая доска «Сцена N»».
- «Это режим просмотра» (14914).

---

## 9. Таблица модалок и того, как они закрываются

| Модалка | Строка | ✕ / кнопки | Escape | Тап по фону |
|---|---|---|---|---|
| Lightbox | 2236 | ✓ | ✓ | ✗ |
| RefPicker | 2687 | ✓ | ✓ | ✓ |
| SearchPalette | 3029 | ✓ | ✓ | ✓ |
| ConfirmModal | 1621 | только кнопки | ✗ | ✗ |
| WelcomeModal | 1653 | только заполнить | ✗ | ✗ |
| DrawingModal | 1763 | ✓ | ✗ | ✗ |
| LightSchemeEditor | 2474 | ✓ | ✗ | ✗ |
| OCR «Рукопись прочитана» | 14614 | ✓ | ✗ | ✗ |
| Дубли «Такие картинки уже есть» | 14651 | ✓ | ✗ | ✗ |
| «Где хранить фотографии» | 14724 | ✓ | ✗ | ✗ |
| «Это режим просмотра» | 14914 | ✓ | ✗ | ✗ |
| «Совместный доступ» | 14946 | ✓ | ✗ | ✗ |
| «На какую доску» | 15129 | ✓ | ✗ | ✓ |
| «Управление проектом» | 15163 | ✓ | ✗ | ✗ |
| «Агент КПП» | 15375 | ✓ | ✗ | ✗ |

**Ни Escape, ни тапа по фону — у 11 модалок.** (Бриф называл 8; расхождение в том, что
OCR, дубли и «На какую доску» там не считались отдельно.)

---

## 10. Дыры, подтверждённые сверкой

1. **«Очистить» в DrawingModal (1841)** — стирает скетч целиком БЕЗ подтверждения.
2. **«Удалить объект» в LightSchemeEditor (2659)** — убирает прибор со схемы БЕЗ подтверждения.
3. **Удаление участника группы (14334)** — БЕЗ подтверждения, в отличие от всех остальных удалений.
4. **Пустое состояние галереи** при нуле референсов отсутствует.
5. **`onError` у `<img>` не задан нигде** (11900, 12162, 13272, 2759) — битая ссылка даёт пустое место без объяснения.

Всего подтверждений удаления в приложении — 17. Три места выше в них не входят.

---

## 11. Технические дефекты оболочки (сверено)

| Дефект | Проверка | Состояние |
|---|---|---|
| `viewport-fit=cover` не задан | строка 5 | подтверждено |
| `apple-mobile-web-app-status-bar-style: black-translucent` | строка 7 | подтверждено |
| `env(safe-area-inset-*)` ждут и не получают | строки 312, 331 | подтверждено, два места |
| Корень на `h-screen`, а не `dvh` | строка 10316 | подтверждено |
| Разъезд брейкпоинтов 699 / 767 / 768 / 1200 | 286, 367, 2929, 9937 | подтверждено |
| `sceneListOpen` от `innerWidth`, один раз | 9910–9911 | подтверждено |
| `locListOpen` от `innerWidth`, один раз | 9878–9879 | подтверждено |
| `scenePanel` от `innerWidth`, один раз | 9921–9922 | подтверждено |
| Подпись референса = имя файла | 8538, 8905 | подтверждено |

---

## 12. Числа для критериев приёмки (замерено на `6b9b528`)

| Что | Команда | Было |
|---|---|---|
| Капслок: классы | `grep -o 'uppercase' index.html \| wc -l` | **34** |
| Капслок: инлайн | `grep -o 'textTransform' index.html \| wc -l` | **11** |
| hover-костыль | `grep -c 'opacity-0.*group-hover' index.html` | **22 места** + правило на 162 |
| Полупрозрачные наложения | `grep -o 'bg-white/[0-9]*\|bg-black/[0-9]*\|border-white/[0-9]*' \| wc -l` | **220** (в 163 строках) |
| Места размера шрифта | `fontSize:` + `text-[Npx]` + `text-xs…3xl` | **597** |
| Ступеней размера шрифта | различных значений `fontSize` | **21** |
| Значений радиуса | `borderRadius:` числом | **8** (4,5,6,7,8,9,10,12) + 5 видов `rounded-*` |
| Токены/старые классы, строки 10000–12999 | `var(--)` / старые | **447 / 21** |
| Токены/старые классы, строки 13000–15400 | `var(--)` / старые | **132 / 328** |
| Размер файла | `wc -c index.html` | **1 260 022** |

Замер «старых классов» считает `bg-white/N`, `bg-black/N`, `border-white/N`,
`text-zinc-N`, `bg-zinc-N`, `border-zinc-N`. Бриф называл 226 и 432 — там был
более широкий шаблон; для сравнимости «до/после» пользоваться шаблоном отсюда.
